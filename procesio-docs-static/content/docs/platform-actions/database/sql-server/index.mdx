---
title: "SQL Server"
icon: "📁"
description: "Run T-SQL queries and commands against a SQL Server database."
---

Two actions: **Execute Query** for statements that return rows, and **Execute Command** for inserts, updates, deletes and anything else that returns only a result.
