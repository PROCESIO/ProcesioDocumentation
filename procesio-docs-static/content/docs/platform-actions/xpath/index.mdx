---
title: "XPath"
icon: "📁"
description: "Actions for querying and updating XML with XPath."
---

**Builder** helps you construct XPath expressions, **Processor** runs them against a document, **Mapper** maps the results to variables, and **Update** writes new values back into the XML.
